function policyDel(id){     
        window.location = `?delpolicy=${id}`;
}
function querryDel(id){     
        window.location = `?delquerry=${id}`;
}
function appDel(id){     
        window.location = `?delapp=${id}`;
}
function appPol(id){     
        window.location = `?appPol=${id}`;
}
function dissPol(id){     
        window.location = `?dissPol=${id}`;
}
function myDownload(){
        window.print();
}